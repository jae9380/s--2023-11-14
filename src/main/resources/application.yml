server:
  port: 8020
spring:
  mvc:
    hiddenmethod:
      filter:
        enabled: true
  threads:
    virtual:
      enabled: true
  thymeleaf:
    cache: false
  h2:
    console:
      path: /h2-console
    datasource:
      url: jdbc:h2:./sb_dev;MODE=MySQL
      driverClassName: org.h2.Driver
      username: sa
      password:
  jpa:
    hibernate:
      ddl-auto: update